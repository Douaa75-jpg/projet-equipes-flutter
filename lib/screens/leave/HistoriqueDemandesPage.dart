import 'package:flutter/material.dart';


class HistoriqueDesDemandesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Historique des demandes")),
      body: Center(child: Text("Voici l'historique des demandes")),
    );
  }
}
